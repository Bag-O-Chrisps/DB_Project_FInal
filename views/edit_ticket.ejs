<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Ticket</title>
    <%- include('partials/cdn') %>
    <link rel="stylesheet" href="/styles.css">
    <style>
        body {
            background-color: #121212;
            color: #ddd;
            font-family: Arial, sans-serif;
        }

        h1 {
            text-align: center;
            color: #007bff;
        }
        label {
            display: block;
            margin-top: 10px;
        }
        .form-control {
            width: 100%;
            padding: 10px;
            margin-top: 5px;
            background: #333;
            color: #ddd;
            border: 1px solid #555;
            border-radius: 5px;
        }
        .btn {
            display: block;
            width: 100%;
            padding: 10px;
            margin-top: 15px;
            background: #007bff;
            color: #fff;
            border: none;
            cursor: pointer;
            border-radius: 5px;
            transition: background 0.3s;
        }
        .btn:hover {
            background: #0056b3;
        }
    </style>
</head>
<body>
    <%- include('partials/navbar') %>
    <div class="container">
        <h1>Edit Ticket</h1>
        <form action="/tickets/update/<%= ticket.ticket_id %>" method="POST">
            <label for="status">Status:</label>
            <select name="status" id="status" class="form-control">
                <option value="Open" <%= ticket.status === 'Open' ? 'selected' : '' %>>Open</option>
                <option value="In Progress" <%= ticket.status === 'In Progress' ? 'selected' : '' %>>In Progress</option>
                <option value="Closed" <%= ticket.status === 'Closed' ? 'selected' : '' %>>Closed</option>
            </select>
            
            <label for="resolution_notes">Technician Notes:</label>
            <textarea name="resolution_notes" id="resolution_notes" class="form-control" rows="5"><%= ticket.notes || '' %></textarea>
            
            <button type="submit" class="btn">Save Changes</button>
        </form>
    </div>
</body>
</html>
